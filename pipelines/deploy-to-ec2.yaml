name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.5.4
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy to EC2
        run: |
          # Replace with your actual EC2 instance details
          ssh ec2-user@your-ec2-instance-ip-or-dns "echo Hello from GitHub Actions!"
          # codebase pull to server
          # set environment variables using .env
          # docker compose up
